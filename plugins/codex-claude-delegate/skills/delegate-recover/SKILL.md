---
name: delegate-recover
description: Recover a stuck Codex delegate loop and prepare the next safe action
---

Run:

```bash
bash "${PLUGIN_ROOT}/scripts/delegate-recover.sh"
```

Then run:

```bash
bash "${PLUGIN_ROOT}/scripts/delegate-status.sh"
```

Report both outputs concisely.
