---
name: delegate-status
description: Show the active Codex delegate loop status and next action
---

Run:

```bash
bash "${PLUGIN_ROOT}/scripts/delegate-status.sh"
```

Report the output directly and keep it concise.
